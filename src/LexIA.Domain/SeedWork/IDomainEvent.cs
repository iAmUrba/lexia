namespace LexIA.Domain.SeedWork;

public interface IDomainEvent
{
    // Marker interface para los eventos del dominio puro.
    // La metadata (quién, cuándo) se maneja externamente en EventMetadata para mantener la pureza.
}
